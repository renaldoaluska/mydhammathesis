# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environment
env/
venv/
.env
.venv

# Git LFS installer (tidak perlu di-commit)
*.tar.gz
*.tar.bz2
*.tar.xz
git-lfs
git-lfs-*/

# HuggingFace cache lokal (bukan artefak kita)
.cache/
~/.cache/huggingface/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Ignored outputs
src/output/
embeddings/

# Yang TETAP di-track (kecil, hasil analisis/eval) ──
# src/output/2-eksplor/   -> statistik + analisis (txt/csv/png) : DI-TRACK
# src/output/5-eval/      -> ringkasan & CSV grade pakar        : DI-TRACK
# (jangan ignore folder ini; hanya artefak biner besar yang di-skip via aturan di atas)
