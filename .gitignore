# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environment
env/
venv/
.env
.venv

# Git LFS installer (tidak perlu di-commit)
*.tar.gz
*.tar.bz2
*.tar.xz
git-lfs
git-lfs-*/

# HuggingFace cache lokal (bukan artefak kita)
.cache/
~/.cache/huggingface/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# ── Artefak BESAR — disimpan di HF Hub, di-sync via src/code/6-sync-hf/ ──
# Embeddings (precompute) & cache pencarian
embeddings/
*.pkl
*.npy

# Korpus mentah SuttaCentral — re-fetch via src/code/1-get-data/
#src/output/1-get-data/sc-raw/
# Base model lokal — re-fetch via 1-get-data; pin revisi tetap di-track
src/output/1-get-data/base-models/*
!src/output/1-get-data/base-models/revisions.json

# Model hasil training (GPL) — re-fetch via 6-sync-hf/2-pull-hf.py
src/output/4-training/models/

# ── Yang TETAP di-track (kecil, hasil analisis/eval) ──
# src/output/2-eksplor/   -> statistik + analisis (txt/csv/png) : DI-TRACK
# src/output/5-eval/      -> ringkasan & CSV grade pakar        : DI-TRACK
# (jangan ignore folder ini; hanya artefak biner besar yang di-skip via aturan di atas)
