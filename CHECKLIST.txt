================================================================================
CHECKLIST & CATATAN KESIAPAN PIPELINE myDhamma
(status restrukturisasi + perbaikan GPL; disusun 2026-06-04)
Ringkasan untuk dosen/penguji: tiap tahap punya flow.txt di folder code-nya, dan
laporan output (rangkuman.txt / verdict.txt) yang grounded ke hasil run.
================================================================================

A. CHECKLIST PER TAHAP
--------------------------------------------------------------------------------
[v] 1-get-data    : 6 script compile OK. base-models = e5/gte/bge (LaBSE DICABUT
                    dari REGISTRY, folder disk, & pin revisions.json). flow.txt ada.
[v] 2-eksplor     : 6 script (urutan natural: parameter > hierarki > analisis-korpus
                    > analisis-lanjutan > simulasi-chunk > simulasi-lanjutan). RUN
                    BERSIH (exit 0). Output: chunk_rules.json (incl. structural_classes
                    & heading_classes), rangkuman.txt. flow.txt ada.
[v] 3-praproses   : 4 script. Chunker: bold-label + class struktural + class heading
                    + (existing) h1-6/header/speaker/kolofon/pts-horner. RUN PENUH.
                    verdict.txt: chunking bersih (4 outlier sah), tagging terbukti,
                    max_seq=384 justified (P99=374 token). flow.txt ada.
[v] 4-training    : 5 script compile OK. SEMUA fix ter-apply (lihat bagian B).
                    flow.txt ada. (Sedang/akan di-RUN — butuh GPU.)
[v] 5-eval        : 6 script compile OK. LaBSE dicabut dari eval_config. Input eval
                    (queries_pakar.json, nama_pakar.json) PINDAH ke src/code/5-eval
                    (EVAL_INPUT_DIR). Bug "queries.json" (selalu kosong) FIXED.
                    INCLUDE_TITLES=False. flow.txt ada.
[v] web/web-md/web-eval : satu sumber chunk (jsonl via _corpus). Invarian sinkron
                    didokumentasi di web/flow.txt. Reader setia ke kanon; docstring
                    diperbaiki (1:1 = chunk_ids; flag heading boleh beda by-design).

B. PERBAIKAN AKAR MASALAH GPL (kenapa tidak akan "jelek seperti dulu")
--------------------------------------------------------------------------------
1. MARGIN-SCALE (biang utama). Dulu: loss MarginMSE mentok ~42, grad_norm meledak
   ~140 karena bi-encoder ST menormalisasi embedding (selisih cosine <=2) sedangkan
   margin CE mentah ~4.5 (max 21) -> target mustahil.
   FIX: 3-pseudo-label.py menskalakan margin ke std target (config.GPL_MARGIN_TARGET_STD
   = 0.25) -> target achievable, MarginMSE bisa konvergen.
2. QUERY-GEN garbled (top_k=10) -> nucleus (top-p) sampling + filter (skrip-asing,
   min-kata, dedup, salinan-verbatim) di 1-query-gen.py. (Group beam search
   deprecated di transformers v5 -> dihindari secara eksplisit via `num_beams=1`
   agar bisa diproses sekuensial cepat dalam 1 panggil. Batch size disesuaikan ke
   BATCH=8 (48 sequence efektif) untuk mencegah Out-Of-Memory di GPU 12GB.
   Generasi MONOLINGUAL: passage id->kueri id, en->en; transfer cross-lingual
   dari base model.)
3. ATTRACTOR VINAYA -> scope CANON-WIDE (config.GPL_SUTTA_ONLY=False): Vinaya IKUT
   diadaptasi (tak ada lagi "pulau" un-adapted) + sampah struktural di-exclude chunker.
   Bukti canon-wide: passages = 189.367 (en 154.838 + id 34.529), naik dari ~132k.
4. BIAS EVAL: putusan base-vs-gpl dari nDCG/precision (3-ndcg/4-precision), BUKAN
   "% kemiripan" cosine (skala-nya bias antar-metode; lihat 5-spearman).

   >> BELUM TERBUKTI sampai di-eval. Saat run training, PANTAU sinyal awal:
      - models/gpl-*/loss_history.json : loss harus TURUN (bukan stuck ~42).
      - grad_norm                       : TIDAK meledak (bukan ~140).
      Jika sehat -> lanjut embed+eval; vonis akhir = nDCG base vs gpl.

C. LaBSE — STATUS: DIHAPUS TOTAL
--------------------------------------------------------------------------------
   - config.REGISTRY (tinggal e5, gte, bge-m3)  - eval_config.json (model_versi/langs)
   - base-models/ disk (tak ada folder LaBSE)   - revisions.json (pin dihapus)
   - embeddings LaBSE (terhapus saat wipe)       - nol referensi fungsional di code
   Alasan: peran "off-the-shelf dense baseline" diwakili bge-m3 (lebih kuat) +
   mengurangi beban anotasi pakar. Boleh disebut di related-work tanpa di-benchmark.

D. URUTAN RE-RUN (hindari jebakan cache/embedding basi)
--------------------------------------------------------------------------------
   2-eksplor (chunk_rules)  ->  3-praproses (jsonl)  ->  4-training (1..4: query-gen,
   hard-neg, pseudo-label, train-marginmse)  ->  5-embed (encode korpus baru)  ->
   5-eval/1-build-cache (search_cache.pkl baru)  ->  WEB grading pakar  ->
   5-eval run.sh (2..6 metrik).
   CATATAN: chunk_rules.json dihasilkan 2-eksplor/6 -> WAJIB jalan sebelum 3-praproses.

E. SINKRONISASI WEB (ringkas; detail di web/flow.txt)
--------------------------------------------------------------------------------
   - SATU sumber chunk = jsonl 3-praproses (via web/_corpus) -> dipakai 5-embed,
     web/search.py, dan web-eval cache. Search semua app konsisten.
   - reader.py (web-md) = chunker paralel dari raw HANYA untuk viewer; 1:1 pada
     chunk_ids/parts (ref search mendarat tepat). Flag heading boleh beda by-design
     (reader tampilkan inline + outline bersih; pipeline exclude utk search/training).
   - Reader setia ke teks kanonik (tampilkan semua, urut asli); yang dilepas hanya
     aparatus editorial (penanda PTS) & boilerplate web.

F. SISA PEKERJAAN (parked; tidak menghalangi run)
--------------------------------------------------------------------------------
   - Query Vinaya untuk queries_pakar.json (lengkapi cakupan eval kanon; perlu vet pakar).
   - Chunker BILARA belum dapat perlakuan class/header seperti chunker HTML -> judul
     bilara bisa bocor sbg body. BENIGN (tersaring GPL_MIN_WORDS=8 di training).
     Lihat src/output/3-praproses/verdict.txt (known-limitation).
================================================================================
